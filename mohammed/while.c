#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * main: using while loop
 * Return: always return 0 (success)
*/
int main(void)
{
	int index = 1;

	while (index <= 5)
	{
		printf("%d\n", index);
		index++;
	}

	return (0);
}
